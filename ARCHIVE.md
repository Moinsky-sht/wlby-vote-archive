# 投票项目退役归档

归档日期：2026-09-15。原站点：vote.wlbycuc.cn。

本仓库是退役源码快照，不会自动部署。src、server、prisma 与包清单来自退役前实际运行的 `h5-vote-preview:phone-share-20260909` 容器，其余构建说明来自服务器工程。原服务器工程比运行容器旧，因此没有直接使用旧目录作为最终源码。

## 恢复方式

- 首选从独立私有 COS 的加密退役归档恢复当时的 Docker 镜像、PostgreSQL dump、上传文件和部署配置。解密凭据单独保存在维护保留目录，不在 GitHub 或 COS。
- 若要重新开发，使用 Node.js 22、pnpm 10.15.1 和 PostgreSQL 16，依据 `.env.example` 单独配置密钥；执行 `pnpm install --frozen-lockfile`、`pnpm prisma:generate`、`pnpm build`。
- 新环境才可按需执行迁移；不要对现有数据库执行重置。运行前检查短信、邮件、上传地址与第三方权限，不复用过期的生产凭据。
- 此次仅归档，没有重新发布或声称完成重建验证；原运行镜像另有留档用于精确恢复。

## 未入库内容

生产密钥、环境文件、个人数据、数据库、上传文件、历史导入明细、内部交接资料、生成的 Prisma 客户端与依赖不上传 GitHub。业务媒体引用可能保留公开资源地址；对应云端资产未在本次退役中删除。
